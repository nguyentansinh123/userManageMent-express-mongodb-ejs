<%-include('include/_header')%>
    <!-- main -->
    <main id="site-main">
        <div class="container">
            <div class="box-nav d-flex justify-beetwen">
                <a href="/add-user" class="border-shadow">
                    <span class="text-gardient">New User <i class="far fa-user"></i></span>
                </a>
            </div>


            <!-- form handling -->
            <form action="/" method="POST">
                <table class="table">
                    <thead class="thead-dark">
                        <tr>
                            <th>ID</th>
                            <th>Name</th>
                            <th>Email</th>
                            <th>Gender</th>
                            <th>Status</th>
                            <th>Action</th>
                        </tr>
                    </thead>

                    <tbody>
                        <%-include('include/_show')%>
                    </tbody>
                </table>
            </form>

            <!-- form handling -->
        </div>
    </main>
<!-- main -->
<%-include('include/_footer')%>